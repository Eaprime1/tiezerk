interface:
  display_name: "Radix"
  short_description: "Repo-specific patterns and workflows for radix"
  default_prompt: "Use the radix repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true